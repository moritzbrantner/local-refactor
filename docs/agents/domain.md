# Domain Context

This repository is a single-context codebase for `local-refactor`.

Before changing behavior, agents should read:

- `CONTEXT.md` for product language and domain terms.
- Relevant ADRs in `docs/adr/` for architectural constraints.

Current ADR directory: `docs/adr/`
