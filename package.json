{
  "name": "local-refactor",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/web",
    "workers/typescript-analyzer"
  ],
  "scripts": {
    "agent:worktree": "./scripts/agent-worktree",
    "dev:service": "cargo run -p local-refactor-service",
    "dev:web": "bun run --cwd apps/web dev",
    "test:core": "cargo test -p local-refactor-core",
    "test:service": "cargo test -p local-refactor-service",
    "test:analyzer": "bun run --cwd workers/typescript-analyzer test",
    "test:web:unit": "bun run --cwd apps/web test:unit",
    "test:web:e2e": "bun run --cwd apps/web test:e2e",
    "test:web": "bun run test:web:unit && bun run test:web:e2e",
    "test:benchmarks": "bun test scripts/benchmark-refactorings.test.ts",
    "check:catalog": "bun run scripts/catalog-check.ts",
    "benchmark:refactorings": "bun run scripts/benchmark-refactorings.ts",
    "check": "cargo test --workspace && bun run check:catalog && bun run test:benchmarks && bun run --cwd workers/typescript-analyzer test && bun run --cwd apps/web build && bun run --cwd apps/web test:unit && bun run --cwd apps/web build-storybook && bun run --cwd apps/web test:e2e",
    "check:llm": "bun run scripts/llm-eval.ts",
    "check:local": "bun run check && bun run check:llm"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14"
  }
}
